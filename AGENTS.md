# 저장소 작업 규칙

## 변경 이력 기록

이 저장소에서 파일을 수정하는 작업은 변경 이력을 함께 관리한다.

- 코드, 설정, 스크립트, 문서를 수정하면 완료 전에 `version-history/CHANGELOG.md`를 갱신한다.
- 해당 문서의 기록 규칙에 따라 한국 시간 날짜와 날짜별 순번, 변경 이유, 실제 변경 내용, 주요 파일, 검증 결과, 커밋·미완료 상태를 적는다.
- 한 작업의 후속 수정은 같은 항목에 보완하고, 별도 작업은 새 항목으로 추가한다. 변경 이력 갱신 자체를 매번 별도 항목으로 반복 기록할 필요는 없다.
- 실행하지 않은 테스트, 적용하지 않은 설정, 수행하지 않은 배포·푸시를 완료로 적지 않는다.
- 읽기 전용 조사만 한 경우 새 변경 항목을 만들 필요는 없다. 기존 작업과 관련된 검증 결과는 해당 항목에 보완할 수 있다.
- 커밋을 만드는 경우 관련 변경 이력도 함께 포함한다. 커밋 ID를 기록하기 위해 재귀적으로 커밋을 만들지 말고 미커밋 작업은 그 상태를 명시한다.
- 변경 이력을 추가하라는 요청만으로 자동 커밋·푸시·배포하지 않는다.

## 변경 이력 문체

CHANGELOG는 대화 기록이 아니라 개발자가 남기는 기술 이력 문서처럼 작성한다.

- `사용자 요청`, `사용자 지시`, `사용자가 ~라고 함`처럼 대화 상대를 주어로 쓰지 않는다.
- 변경 이유는 `요구사항 반영`, `시연 중 확인된 이슈`, `운영 기준 변경`, `정합성 보완`, `검증 필요`처럼 개발 배경과 기술적 목적 중심으로 적는다.
- 요청 문장을 그대로 인용하지 않는다. 원문 자체가 계약·정책·오류 재현 조건으로 꼭 필요한 경우에만 짧게 남긴다.
- 작업 주체를 굳이 적어야 할 때는 `개발자`, `운영 담당`, `관리자`, `클라이언트`, `서버`처럼 실제 역할을 사용한다.
- `사용자 ID`, `사용자 식별`, `사용자 PC`처럼 시스템 도메인에서 실제로 사용자를 뜻하는 용어는 그대로 사용한다.
- 가능하면 각 항목을 `이유 / 변경 / 주요 파일 / 검증 / 상태` 순서로 정리하고, 사실과 미실행 항목을 구분한다.

예시:

- 피함: `이유: 사용자 요청 "TEXT 팝업에서 Markdown 빼줘".`
- 권장: `이유: TEXT 팝업에서 Markdown 기능을 사용하지 않기로 확정해 관련 코드와 의존성을 제거.`
- 피함: `이유: 사용자가 서버쪽 확인을 요청함.`
- 권장: `이유: 서버 API와 Oracle 연동 동작을 실제 HTTP 기준으로 검증할 필요가 있어 통합 테스트 수행.`

