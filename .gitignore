
zero-rule-web/main/.env
zero-rule-web/main/.env.production

# WPF 폐쇄망 복원/검증 시 생성되는 로컬 캐시와 게시 결과
.offline-cache/
.offline-verify/
popup-frameWork/Popup/bin/
popup-frameWork/Popup/obj/
popup-frameWork/publish/

# [proto 저장소] sample 저장소의 대용량 바이너리는 커밋하지 않는다 (OFFLINE_WPF_BUILD.md 참조, 필요 시 sample에서 복사)
.vs/
offline-sdk/*.exe
offline-packages/nuget/*.nupkg
popup-frameWork/dist/
zero-rule-server/.idea/
# 업스트림 zero-rule-server(git.labcl.net/clover/zero-rule-server @56bb0c5) 클론의 .git을 보관용으로 이름만 바꿔 둔 것 — 커밋하지 않는다
zero-rule-server/.git-upstream-*/
zero-rule-web/node_modules/
zero-rule-server/**/build/
zero-rule-server/.gradle/
offline-export/

# 폐쇄망 반입용 VS Code 확장(.vsix) — 개당 최대 131MB로 GitHub 파일 제한을 넘고,
# 위 "대용량 바이너리는 커밋하지 않는다" 규칙과 같은 이유로 저장소에 넣지 않는다.
offline-extention/
# Claude Code 작업용 worktree 체크아웃(임시)
.claude/worktrees/
